# Base URL of the external microservice gateway (default: http://localhost:8080)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Enable ?state= URL param preview variants (error/loading/empty).
# Keep false in production.
NEXT_PUBLIC_ENABLE_DEMO_STATES=false